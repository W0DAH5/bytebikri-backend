<?xml version="1.0" encoding="utf-8"?>
<!--
  Cleartext is off everywhere, including the local development server.

  There is exactly one exception, and it is scoped to the loopback addresses a
  developer uses: 10.0.2.2 is the host machine as seen from the emulator, and
  localhost is the device itself. Content URLs are signed and short-lived, but a
  session cookie travels on every request — over plain HTTP on a network that is
  not a loopback, that cookie is the account.

  Release builds talk to one https origin, set by `BASE_URL` in build.gradle.kts.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
